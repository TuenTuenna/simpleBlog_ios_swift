# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SimpleBlog' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SimpleBlog
  pod 'Alamofire', '~> 5.0.0'
  pod 'Floaty', '~> 4.2.0'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'FFPopup'
  pod 'SwiftEntryKit', '1.2.3'
  pod "SwiftyCam"
end
